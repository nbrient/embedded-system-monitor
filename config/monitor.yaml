generic_config:
  log_method: journalctl

cpu_config:
  enable_acquisition: 1
  timer: 5

mem_config:
  enable_acquisition: 1
  timer: 5

irq_config:
  enable_acquisition: 1
  timer: 5

proc_config:
  enable_acquisition: 1
  timer: 5
